# Contributing

Thanks for contributing to jira2mcp.

## Development checks

This project uses `uv` and the repository `Makefile` for local development commands.

Before opening a pull request, run:

```bash
make check
```

This runs the lint, format, and type-check targets.

To see the available development targets, run:

```bash
make help
```
